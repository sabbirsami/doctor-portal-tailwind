import React from "react";

const Loading = () => {
    return (
        <div className="h-[90vh] flex justify-center items-center">
            <button class="btn btn-square loading btn-xl"></button>
        </div>
    );
};

export default Loading;
